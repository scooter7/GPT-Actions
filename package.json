{
  "name": "gpt-auth-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "postinstall": "npm install --prefix client"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}